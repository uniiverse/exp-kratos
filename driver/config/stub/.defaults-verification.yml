selfservice:
  methods:
    password:
      enabled: true

  flows:
    settings:
      privileged_session_max_age: 1m

    verification:
      enabled: true
      lifespan: 5s
      after:
        default_browser_return_url: https://uvrs.tech:4455/

    logout:
      after:
        default_browser_return_url: https://uvrs.tech:4455/auth/login

identity:
  default_schema_id: default
  schemas:
    - id: default
      url: file://test/e2e/profiles/verification/identity.traits.schema.json
